apiVersion: v1
kind: Service
metadata:
  name: devops-pipeline-service
spec:
  type: NodePort
  selector:
    app: devops-pipeline-app
  ports:
    - port: 3000
      targetPort: 3000
      nodePort: 30080
